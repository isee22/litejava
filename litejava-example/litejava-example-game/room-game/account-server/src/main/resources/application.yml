server:
  id: account-1
  httpPort: 8101

app:
  version: "1.0.0"
  priKey: game_secret_key

# Redis 配置 (集群模式)
redis:
  host: localhost
  port: 6379
  password:

mybatis:
  url: jdbc:mysql://localhost:3306/game?useSSL=false&serverTimezone=UTC&characterEncoding=utf8&allowPublicKeyRetrieval=true
  username: root
  password: 123456
  poolMaxSize: 10
